<!--components/select/companysize.wxml-->
<picker bindchange="save" value="{{index}}" range="{{list}}" range-key="{{'name'}}">
    <view class="picker">
        <input type="text" class="weui-input" placeholder="请选择内容" value="{{pickerData?pickerData:''}}" disabled/>
    </view>
</picker>
